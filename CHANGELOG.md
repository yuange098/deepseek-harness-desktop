# 更新日志

## 2026-09-24 — 从零搭起：安装、插件、桌面壳、主题

**安装**
- DSH 核心装到 `E:\DeepSeekHarness`（`@deepseek-ai/dsh` 的 `next` 标签，实测 `0.1.7-rc.2`；`latest` 上没有这个包）。
- 抓取 npm + GitHub 的 DSH 插件数据并按热度排序（`tools/plugins/`），挑出前几名安装，**同功能只留一个**，最终启用 10 个插件（市场、侧边工作台、上下文、记忆、通知、模型对比、搜索、归档等）。

**桌面封装**
- Electron 壳：自动拉起 `dsh web`、原生窗口按钮、可缩放、单实例锁、托盘、启动前清理残留服务。
- 图标换成 DeepSeek 鲸鱼（黑色版）；启动画面从"矩形窗口 + 小图标"改成"屏幕中央的图标 + 进度条"，并解决"启动画面没消失主界面就出现"的重叠问题。
- 菜单栏「帮助 → 检查更新…」：比对 DSH 核心与插件的 npm 版本，只提示、不自动改。

**主题与外观**
- 自写插件 `dsh-harness-site`：把官网设计变量覆盖到客户端（浅色 `#f9f8f8` / 深色 `#0a0a0a`、品牌蓝 `#4d6bfe`、主按钮近黑 `#1a1615`）。
- 试过整窗半透明玻璃效果（含分层透明度），但出现**启动时延迟可见、开始菜单/右下角菜单点开后局部变暗**等不稳定现象 → 按"稳定性优先"整段回退成不透明标准窗口。

## 2026-09-25 — 界面细节、代码模块、侧栏容器

**界面微调**
- 「上下文洞察 / 设置」并排到侧栏底部，前者改名「洞察」。
- 自己发出的消息气泡改成淡蓝底 + 蓝色描边。
- 右侧面板宽度**全局记忆**：官方按会话存，换会话就回默认；插件镜像成全局值，拖一次到处生效。
- 客户端补上 Electron 右键菜单：选中文字复制、输入框剪切粘贴、图片复制、链接打开。

**新插件 `dsh-code-history`（代码页签）**
- 第一版：把会话里的代码收成卡片，分「执行 / 回答」两类，自动起中文标题。
- 性能修复：一次长会话任务后界面卡顿 → 改成原始类型订阅 + 500ms 防抖 + 跳过 reasoning 大文本。
- 结构修复：过程性代码文件按任务归并，不再平铺一层；视图从"文件夹式"改成模块化卡片（圆角小长方形，点击展开）。
- 交互补齐：标题可双击改名（失焦自动保存 + 恢复）、整体/选择性复制、导出 Markdown、搜索、缩略图、摘要、定位对话、修改轨迹、合并成可运行版本。
- 细节：进页签隐藏输入框、标题中文化、卡片一排 2~4 个自适应。

**Skill 打通**
- 把 Codex 的 skills 清单化、规划、junction 软链进 DSH 的 `home/skills`，并写校验脚本确认链接没坏（`tools/skills/`）。

**新插件 `dsh-session-tools`（删除对话）**
- 需求：给每个对话一个删除入口，删前弹窗警告"不能恢复"，确认后清掉该对话的全部记录数据。
- 发现：官方客户端 API 里**没有**删除会话的方法（只有 create/rename/archive/list…），
  workspace 侧只有"删整个工作区"，所以删除只能落到文件系统。
- 实现：会话行悬停出现垃圾桶按钮（塞进官方 `rowActions` 槽位）→ 确认弹窗 →
  `preload` 暴露的 `window.dshDesktop.deleteSession` → IPC → 桌面壳按 id 精准删数据。
- 删除范围（实测定位）：`home/sessions/<工作区>/<会话id>/`、`home/storages/session_projcache/sessions/<id>.json`，
  并从 `home/storages/workspace.json` 摘掉登记；内容寻址的共享附件库不动。
- 安全：id 必须 UUID 形态、每个目标做包含性校验（拒绝路径穿越）、原子写登记表；
  删除逻辑拆成纯 Node 模块 `desktop/src/session-store.js`，配 9 项单元测试（`tools/publish/test-session-store.js`）。
- 验收：UI 探针确认"按钮在位 / 弹窗含无法恢复文案 / 取消不删 / preload→IPC→删除模块链路 dry-run 通过"。
- 追加：删除前显示规模（轮次 / 步骤 / 磁盘占用 / 输出 tokens），数据取投影缓存 + 目录体积；
  删「当前打开的对话」时额外提示"列表可能残留一条，重启即清"；单元测试扩到 11 项。

**修掉"鼠标划过就能拖聊天框宽度"**
- 取证（`tools/probes/probe-splitters.cjs`）扫出 4 个 resize 元素，其中两个是官方
  `dsh-client-ui-conversation` 的 `widthHandle`（10×842px、`cursor:col-resize`、跟着
  `--dsh-chat-content-width` 定位）—— 就是"能来回拖动聊天框宽度"的那个东西。
- 处理：主题插件注入 `[class*="widthHandle"]{display:none !important}`，只藏手柄本身，
  不动容器与布局；右侧面板那根把手（"拖一次记住宽度"用的）刻意保留。
- 验收：手柄尺寸变 0×0（碰不到），右侧把手仍在（8×918），会话行与「删除对话」按钮无异常。

**桌面壳两份代码的坑（真实踩过）**
- 现象：插件里的删除按钮一直提示"当前不是在桌面客户端里打开的"。
- 根因：改的是 `desktop/src/`，而双击的 exe 读的是 `resources/app/src/`。
- 处理：新增 `tools/publish/ship-desktop.ps1`（同步 src+assets → 校验打包版含新代码 → 重启）；
  桌面壳启动时也往日志写一行 `[desktop] 会话 IPC 已就绪` 便于自检。

**代码页增加"任务意图过滤"（用户提的逻辑问题）**
- 问题：上传文档让 AI 写文字时，它顺手跑的处理脚本、回答里夹的代码块，都不是用户想要的成果，
  却混进了代码模块；而"问代码、处理数据、画图表"的代码才是要归档的。
- 做法：从轨迹里取**用户提问原文**（`kind:"user"` 块，`content` 字段；块本身不带 turn，
  交给它后面第一个 turn 认领），用中文关键词权重打分判 `code` / `text`；`text` 任务的代码默认折叠。
- 顺带修好一处数据缺口：回答里的代码原先没有 turn，无法判断归属 —— 现在同时从轨迹的助理正文
  （`kind:"text"`）抽一遍围栏代码并带上 turn，与聊天快照抽到的按 `语言|代码` 去重合并。
- 验收：17 条真实句式（写引言/润色/翻译/汇报稿/数据整理成文档 vs python/报错/合并统计/箱线图/
  热力图/山脊图/重命名脚本…）判定全中；合成轨迹测试确认文字任务→折叠、绘图任务→保留。
- 取证脚本：`tools/probes/probe-intent.cjs`、`tools/probes/probe-intent-synth.cjs`。

**右侧面板：从"改按钮"到"改容器"（本日最后一段）**
- 误把"容器太大"理解成"按钮太大" → 先把按钮压到 36px 高，用户指出方向错了。
- 取证后确认：面板宽度由 app 自己的布局状态决定（`--dsh-sidebar-width` + 占位列 `rightbarCol` + 拖拽把手）。
- 硬写内联宽度失败两次（面板表面与内容层不一致 → 右侧留白；纵向 flex 写 `flex-basis` → 内容层被压成正方形）后，改成**合成拖拽**：由插件替用户拖一次把手，让 app 自己把整条布局算对。
- 最终：按钮尺寸 **253×50**（宽 380→253，缩小 1/3；高 36→50），容器**自动**跟着变成 300px（app 最小宽度），聊天区同步变宽，把手位置自动跟随；用户手动拖宽度不被插件拉回。
- 顺带修掉一个隐藏 bug：右栏是收起状态启动时，面板 DOM 晚挂载，样式只在启动后试装三次会漏 → 补 `MutationObserver`。
